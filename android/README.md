# WakeLink Android App

Android-клиент для WakeLink — системы удалённого управления Wake-on-LAN через ESP8266/ESP32.

## Возможности

- 🔗 Подключение к устройствам через TCP (LAN) и Cloud (HTTP/WSS)
- 🔐 End-to-end шифрование ChaCha20 + HMAC-SHA256
- 📱 Современный Material Design 3 интерфейс
- ☁️ Управление режимом Cloud на устройстве

## Команды

| Команда | Описание |
|---------|----------|
| **Ping** | Проверка связи с устройством |
| **Info** | Информация об устройстве (IP, RSSI, память) |
| **Wake** | Отправка Wake-on-LAN пакета по MAC адресу |
| **Restart** | Перезагрузка ESP устройства |
| **Cloud On** | Включить WSS подключение к серверу |
| **Cloud Off** | Отключить WSS подключение |
| **Cloud Status** | Статус облачного подключения |

## Требования

- Android 7.0 (API 24) или выше
- Android Studio Hedgehog или новее
- JDK 17

## Сборка

```bash
cd android
./gradlew assembleDebug
```

APK будет в `app/build/outputs/apk/debug/app-debug.apk`

## Структура проекта

```
android/
├── app/
│   ├── src/main/
│   │   ├── java/org/wakelink/android/
│   │   │   ├── crypto/         # ChaCha20 + HMAC
│   │   │   ├── protocol/       # Протокол v1.0
│   │   │   ├── data/           # Модели и репозиторий
│   │   │   ├── network/        # TCP, HTTP, WSS клиенты
│   │   │   └── ui/             # Compose UI
│   │   └── res/                # Ресурсы
│   └── build.gradle.kts
├── build.gradle.kts
└── settings.gradle.kts
```

## Безопасность

- Токены хранятся в EncryptedSharedPreferences
- Все команды шифруются ChaCha20
- HMAC-SHA256 для проверки целостности
- Протокол v1.0 совместим с firmware и Python клиентом

## Лицензия

NGC License v1.0 — только персональное использование.
